include 'subprojects/griffon-fatcowicons-core'
include 'subprojects/griffon-fatcowicons-swing'
include 'subprojects/griffon-fatcowicons-javafx'
include 'subprojects/griffon-fatcowicons-swing-groovy'
include 'subprojects/griffon-fatcowicons-javafx-groovy'
include 'subprojects/griffon-fatcowicons-guide'

rootProject.name = 'griffon-fatcowicons-plugin'
rootProject.children.each { project ->
    int slash = project.name.indexOf('/')
    String fileBaseName = project.name[(slash+1)..-1]
    String projectDirName = project.name
    project.name = fileBaseName
    project.projectDir = new File(settingsDir, projectDirName)
    project.buildFileName = "${fileBaseName}.gradle"
    assert project.projectDir.isDirectory()
    assert project.buildFile.isFile()
}
